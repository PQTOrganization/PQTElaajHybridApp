import React from "react";
import { LoadingButton } from "@mui/lab";

const LoadingButtonDX = (props: any) => {
  return <LoadingButton variant="contained" {...props} />;
};

export default LoadingButtonDX;
